from __future__ import annotations

# Celery app init — configure broker via settings.redis_url
# from celery import Celery
# from app.config import settings
# celery_app = Celery("qa_hub", broker=settings.redis_url)
